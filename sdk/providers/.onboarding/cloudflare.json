{
  "runbook": "onboard-provider",
  "target": "cloudflare",
  "started": "2026-09-03",
  "updated": "2026-09-03",
  "hops": [
    {
      "id": "spec-discovery",
      "status": "done",
      "note": "Real, public OpenAPI 3.0.3 spec confirmed at github.com/cloudflare/api-schemas, openapi.yaml (18,310,953 bytes as fetched today -- the 16.8MB the ticket cited has already grown, confirming the spec really is updated regularly). Loaded via internal/openapi.Load directly, no redocly_bundle needed -- unlike DigitalOcean, plain Load succeeded against the unbundled document. Re-run today with a fresh scratch tool (candidate-provider-survey.md's own one-shot tool was not committed, so a new one was built, same method: wraps openapi.Load + resourcemap.Discover/DiscoverDataSources, built inside ubx-provider-dynamic's own module tree under cmd/scratch-cf-discover since Go's internal-package visibility rule blocks a separate module even with a replace directive -- deleted after this hop). Real, live counts today: 2150 paths, 307 resources, 721 data sources, 1028 total -- close to but not identical to the survey's own 2123/307/714/1021 (real drift, consistent with a regularly-updated upstream spec, not a discrepancy to chase).\n\nReal finding, new, not covered by anything currently in onboard-provider.md: 34.5% of discovered resources (106/307) and 44.8% of data sources (323/721) have a TypeName matching a generic-response-wrapper pattern (deriveNoun's own response-schema-name heuristic, resourcemap.go's own doc comment calls this 'the strong, real signal' -- weak for Cloudflare specifically). Cloudflare's own spec authors reuse a small set of generic response envelope schema component names (single_response, id_response, single_response_collection, ...) across hundreds of structurally distinct real resources instead of giving each its own uniquely named schema component -- confirmed by reading real examples: cloudflare_access_single_response_2 through _22 are 15 genuinely distinct real Access sub-resources, disambiguated only by an appended collision counter, not a bug in the collision resolution itself. Not a loader gap (the spec loads and produces real, distinct resources) and not a correctness bug (nothing is silently merged that shouldn't be) -- a real, provider-specific naming-quality issue that will make write-artifacts categorization/description authoring materially harder for roughly a third to a half of Cloudflare's own wire types, the same shape of thing hop 3's own DigitalOcean grouping warning already exists to catch early, just one level lower (noun quality within a service, not service grouping itself). Recording here per UBI-222's own explicit instruction to record every point needing intervention as a runbook gap; adding a corresponding check to onboard-provider.md's own hop 3 in the same PR as this manifest, rather than working around it silently.\n\nDistinct operation tags: 551, real and human-authored (Email Security Settings, Radar HTTP, R2 Bucket, Threat Signals, Workers for Platforms, Zone Settings, ...) -- a real, usable grouping signal exists, same shape as DigitalOcean's own tags that namespace_from_tags already reads. Checking whether namespace_from_tags should be enabled for cloudflare is hop 3's own job once the live entry exists (hop 2); noted here as a positive signal, not decided yet."
    },
    {
      "id": "live-entry",
      "status": "done",
      "commit": "https://github.com/Ubiquex/ubiquex/pull/68",
      "note": "sdk/providers/.ubx/config carries [dynamic_providers.cloudflare], live: schema_source=openapi, schema_url=github.com/cloudflare/api-schemas' own raw openapi.yaml, base_url from the spec's own declared servers entry (api.cloudflare.com/client/v4), namespace_from_tags=true (see loader-check below for why). No redocly_bundle needed."
    },
    {
      "id": "loader-check",
      "status": "done",
      "note": "Real ubx sdk gen --only cloudflare --dump-ir run against the live entry, real output: 1088 resource type(s), 14,862 real fields, 5,305 sourced (36%), 0 AI-inferred, 9,557 genuinely undescribed (64%). Two real, confirmed-live bugs found and fixed in ubx-provider-dynamic before this succeeded, neither worked around -- see ubx-provider-dynamic PR #59 (open, not merged) for the full account:\n\n1. deriveNoun's path-based fallback crashed the run outright the first time: a data source path ending in a literal '-' segment singularized to an empty string, producing a bare 'cloudflare_' TypeName that failed sdk/codegen/ir's own ServiceAndLocalName ('wire type must be at least <provider>_<service>'). Root cause was general, not a one-off: the fallback's scan started one segment before the read path's own true last segment, correct only when that last segment is always a {param} (true for a single-item resource path, never checked for a collection-shaped data source path). Also silently produced wrong nouns for several real Cloudflare types before the crash was even hit ('ai' instead of 'finetune', 'd1' instead of 'database', 'kv' instead of 'namespace'). Fixed at the root (start the scan at the true last segment), new regression test, real effect: 721 -> 834 real data sources once the run stopped losing 113 of them to a shared wrong noun.\n\n2. The real service/namespace grouping check hop 3 asks for was UNREACHABLE as the runbook and the tool stood: namespace_from_tags (the flag meant to fix exactly this) had no effect at all for a live, unpinned OpenAPI provider -- confirmed by an identical before/after schema.json diff with the flag toggled, then traced to the real cause: the live --dump-namespaces branch returned an unconditional empty map, never reading cfg.NamespaceFromTags, because that flag was only ever wired into the snapshot-GENERATION path (used at hop 4/5), not the live single-provider path every still-live provider actually runs. Fixed at the root so the live path now honors the flag too, mirroring the snapshot path's own real algorithm.\n\nWith that fix in hand, the real grouping check: mechanical wire-name split alone produces 362 distinct services from 1088 real wire types, 198 of them (54.7%) single-resource -- the same real fragmentation problem hop 3's own DigitalOcean example describes, confirmed for real on Cloudflare too. Tag-based grouping (namespace_from_tags=true) is a genuine but MIXED real result, unlike DigitalOcean's clean win: 443 distinct namespaces (more than the mechanical split, not fewer -- Cloudflare's own 551 real operation tags are simply finer-grained than its 1088 real wire types support as a clean product taxonomy), but single-resource namespaces drop to 150 (33.9%, down from 54.7%) and the namespace names themselves read as real Cloudflare products more often (r2bucket, cloudflaretunnel, zonesettings, magicconnectors, emailsecuritysettings) than the mechanical split's own coarser, sometimes-generic groups (access, tls, magic, iam, zero, custom, resource). Judgment call, not a clean-cut measurement: enabled (namespace_from_tags=true, see live-entry above) on the strength of the single-resource-fragmentation reduction, the specific problem this check exists to catch, accepting the total-group-count tradeoff -- flagged here explicitly rather than presented as an unambiguous win, for the founder to revisit if write-artifacts finds the tag-based groups less usable in practice than the mechanical ones would have been."
    },
    {
      "id": "schema-repo-create",
      "status": "done",
      "note": "Real, hard blocker, not worked around: --generate-snapshot-group refuses to write a real snapshot (min_binary_version would be the untraceable \"dev\" default) because the binary available right now is a local build off ubx-provider-dynamic PR #59 (open, not merged) -- the two hop-3 fixes this run genuinely needs. Generating from the last real released version (v1.0.7) would silently reintroduce both: the deriveNoun crash/mis-naming bug and the dead namespace_from_tags flag. Not a real option.\n\nAlso found and fixed in the runbook itself (ubx-provider-runbook PR #17, open): hop 4's own text named the wrong generation command entirely (ubx sdk gen --dump-ir produces the docs IR shape, not a real schema snapshot), and separately, --generate-snapshot-group cannot run against the real, full sdk/providers/.ubx/config once any other provider is already pinned (internal/config.Load requires schema_source on every table, no notion of the pinned source/version shape) -- confirmed by running it against a scoped, temporary config carrying only cloudflare's own table instead, which correctly produced both real members (cloudflare, cloudflare_ds) before hitting the min_binary_version refusal.\n\nReal path forward: ubx-provider-dynamic PR #59 needs review and merge, then a real publish.yml dispatch to cut a new release, then this hop re-run against that released binary. Waiting on that before continuing -- not self-merging content that needs judgment.\n\nUnblocked: ubx-provider-dynamic PR #59 merged, v1.0.8 released and independently verified (checksum, and both fixes confirmed present by running the released binary directly against the real Cloudflare spec before using it for anything). Real snapshot generated from that exact release: 268 resources + 820 data sources = 1088 wire types, matching the full ubx sdk gen pipeline's own earlier count exactly. ubx-schema-cloudflare created, pushed, verified via a fresh independent clone -- manifest.json and both members/*.json present and correct, min_binary_version=1.0.8.\n\nReal runbook gap found and fixed while building this repo (ubx-provider-runbook, same PR as the hop 4 command fix): hop 4's own text said nothing about the real files a ubx-schema-<provider> repo needs beyond manifest.json/members/*.json -- LICENSE, README.md, CLAUDE.md, STATE.md, HISTORY.md, and five .github/workflows/*.yml files (hash-watch.yml, publish.yml, ci.yml, orphan-branch-watch.yml, stale-base-check.yml) plus scripts/orphan_branch_check.py, all reconstructed from ubx-schema-github's own real, working copy since hop 4 never named any of them. The identical class of gap hop 6 already documents for SDK repos, just never previously written down for the schema repo hop.\n\nAlso hit, live, the exact 'a stacked PR is only safe while its base is unmerged' trap this project's own TRAPS.md already documents: PR #68 (hops 1-3) was merged by the founder while a further commit (this hop's own 'blocked' status) sat pushed on top of the same now-merged branch -- landed nowhere near main until caught by fetching fresh and checking ancestry, exactly the trap's own prescribed check. This entry recovered onto a fresh branch off current main.",
      "commit": "https://github.com/Ubiquex/ubx-schema-cloudflare"
    },
    {
      "id": "release-cut-and-pin",
      "status": "done",
      "note": "Real v1.0.0 GitHub Release cut via ubx-schema-cloudflare's own publish.yml (workflow_dispatch), independently verified: checksum of the downloaded snapshot.tar.gz recomputed and matched against the real SHA256SUMS asset, extracted content byte-identical to what was pushed, exactly two real assets (snapshot.tar.gz, SHA256SUMS), never trusted from the workflow's own exit status.\n\nsdk/providers/.ubx/config's own [dynamic_providers.cloudflare] switched from live (schema_source/schema_url/base_url/namespace_from_tags) to pinned (source=ubiquex/cloudflare, version=1.0.0) -- within the SAME table, matching the real, already-corrected hop 5 text, not a separate [providers.cloudflare] table."
    },
    {
      "id": "sdk-repo",
      "status": "done",
      "note": "ubx-sdk-cloudflare created, public, real Go/Python/TypeScript bindings generated against the pinned v1.0.0 schema (ubx sdk gen --lang go/py/ts against ubx-provider-dynamic v1.0.8, then ubx sdk init-repo for the repo scaffold). Verified before commit, not assumed: go build + go vet clean (after a real go mod tidy), all 1748 python files parse clean (ast.parse), deno check clean across the full typescript tree (after a real deno install against the live npm registry). Initial scaffold committed direct to main, matching every other real repo's own confirmed first-commit precedent. Verified via a fresh independent clone.\n\nReal tool bug found and fixed at the root (ubiquex, same repo this whole onboarding already has an open PR against): ubx sdk init-repo's own publish.yml template bundled exclusions.json into the same required cp as descriptions/intros/categories -- a genuinely new provider has no exclusions.json yet, and the generated workflow would have hard-failed the 'Package the docs artifact' step outright the first time publish.yml actually ran. ubx-sdk-digitalocean's own real, published copy already carries this exact fix by hand (a separate cp with 2>/dev/null || true), but the template itself had never been updated to match -- confirmed live: without this fix, cloudflare's own freshly-generated publish.yml had the same bug. Fixed in sdk/codegen/templates/repo/publish.yml, new regression test (repo_test.go) proving the generated output matches DigitalOcean's own real, working pattern, full sdk/codegen test suite passes. Confirms the user's own thing to watch for: the templated docs-artifact step and PR-based version bump ARE real and correct in the tool now, but only after this fix -- neither had actually been exercised against a genuinely new repo since DigitalOcean's own hand-patch, so the drift had gone unnoticed.\n\nReal, deliberate first version chosen: 1.0.0 (matching the schema pin's own starting version), sdk/typescript/package.json, sdk/typescript/deno.json, sdk/python/pyproject.toml all bumped together.\n\nGenuinely blocked, not worked around: NPM_TOKEN and PYPI_TOKEN are not available to this new repo (confirmed via gh secret list -- empty -- and the org-secrets API, which this session's own token cannot query without admin:org, itself informative: this really does need a human with the real npm/PyPI account, exactly as onboard-provider.md's own hop 6 already says). publish.yml has not been dispatched -- doing so now would fail with a real ENEEDAUTH. Waiting on the founder to provision both as real, repo-scoped secrets (Settings -> Secrets and variables -> Actions) before this hop can finish.\n\nUnblocked: access lists updated for NPM_TOKEN/PYPI_TOKEN, confirmed via the repo-scoped organization-secrets endpoint before dispatching anything. publish.yml dispatched, every step succeeded including the 'Package the docs artifact' step (confirming the exclusions.json fix above works for real, not just in the test). Verified independently, not from the workflow's own exit status: npm (both the specific-version endpoint and -/package/.../dist-tags, 1.0.0), PyPI (1.0.0, real wheel + sdist), Go module proxy (sdk/go/v1.0.0, real origin hash matching the pushed commit), and the docs release (checksum verified, byte-identical content, artifacts/ correctly has no exclusions.json -- the tolerant cp did exactly what it was supposed to). The workflow's own 'Verify registries agree' step and 'Open a PR for the version bump' step (correctly a no-op: committed and published versions were already identical, real first publish) both confirmed by reading their own real log output, not assumed from a green checkmark."
    },
    {
      "id": "docs-site",
      "status": "done",
      "commit": "https://github.com/Ubiquex/ubx-docs-providers/pull/14",
      "note": "One entry added to ubx-docs-providers' own config/providers.json, matching every existing entry's shape. No goModule override needed -- sdk/go/go.mod has no version suffix.\n\nConfirmed live via a real npm run build, not a config diff that merely looks plausible: provider home page renders (real Cloudflare heading, real per-service directories from namespace_from_tags), a real resource detail page renders correctly (cloudflare_data_class). Real sidebar coverage: 0/1088 categorized -- correct and expected, not a bug. artifacts/cloudflare/categories.json is intentionally empty (hop 6's own scaffold), and the site's own honest-coverage design (UBI-245) surfaces every uncategorized wire type as Uncategorized rather than guessing at a label. Onboarding is complete as of this hop, per onboard-provider.md's own text -- hop 8 (write-artifacts, real content authoring for all 1088 wire types) is optional polish, explicitly held per the founder's own instruction for its own separate checkpoint, given its real scale."
    },
    {
      "id": "write-artifacts-categories",
      "status": "done",
      "commit": "https://github.com/Ubiquex/ubx-sdk-cloudflare/pull/1",
      "note": "Optional (hop 8), begun per explicit founder instruction, its own checkpoint given the real scale (1088 wire types). Categories first, using the UBI-245 token method. Real classification reported before authoring anything: categories.json started genuinely empty, so 0 clean, 0 ambiguous, 444 no-signal. Canonical merge (same service spelled two ways) found zero collisions -- one source, no dual-spelling situation to catch.\n\nReal signal used: Cloudflare's own 551 human-authored OpenAPI tags, the same signal namespace_from_tags already reads for wire grouping. 426 of 444 tokens already carried a genuine product name, normalized to consistent title case. 18 carried an internal, non-human tag (dos-flowtrackd-api_other, ppc_config, bare words like content or policies) and were individually traced to their real wire types and paths, collapsing onto 9 real Cloudflare products.\n\nReal result: 444 tokens collapse to 433 distinct category labels for 1088 wire types. Verified 100% coverage two ways: direct computation, and a real ubx-docs-providers sidebar build against a local mirror (433 groups, 1088/1088 categorized, exact match). Deliberately not forced coarser -- 433 for 1088 is finer-grained than AWS or Azure, but reflects Cloudflare's own real product taxonomy, not artificial fragmentation.\n\nReal runbook gap found and fixed (ubx-provider-runbook PR #19): MANIFEST.md documented write-artifacts' own .artifact-manifest.json as already migrated to each real ubx-sdk-<provider> repo's own root -- confirmed live, this never actually happened anywhere. Cloudflare's own categories batch created the first real one."
    },
    {
      "id": "write-artifacts-intros",
      "status": "in_progress",
      "note": "Started, stopped almost immediately: the first real wire type read closely for its own intro (cloudflare_abuse_report) had fields about moving accounts between organizations, nothing to do with abuse reports. Traced to a real, serious bug in ubx-provider-dynamic's own findCreate, not a docs issue -- its inline-schema fallback matched two operations as a create/read pair purely because they share Cloudflare's own generic response envelope property names, ignoring that the wrapped real entity type differs completely. cloudflare_abuse_report's own real create was bound to POST /accounts/move ('Batch move accounts... Not implemented,' per its own real description).\n\nNot isolated: comparing real Discover() output before and after a fix, 19 real resources (cloudflare_workflow, cloudflare_organization, cloudflare_pipeline, cloudflare_stream, and 15 more) had their own real create operation silently bound to the wrong endpoint the same way, and 4 more (cloudflare_abuse_report, cloudflare_physical_device, cloudflare_registration, cloudflare_tenant) turn out to have no real create at all once the false match is removed -- genuinely read-only resources this bug had wrongly made appear creatable.\n\nThis affects the already-published v1.0.0 ubx-schema-cloudflare snapshot and ubx-sdk-cloudflare bindings directly, not just future docs -- a real infrastructure-correctness bug, not a content gap. Fixed and tested (ubx-provider-dynamic PR #60, v1.0.9). Schema regenerated as v1.0.1, ubiquex pinned, SDK regenerated, categories.json's 4 stale keys removed, all verified independently.\n\nSecond, distinct bug found immediately on resuming: v1.0.9's fix only helps when at least one side of a candidate pair names its wrapped entity via a real $ref. Cloudflare's own dominant real pattern is BOTH sides lacking one entirely -- that check has nothing to compare and silently degrades back to name-only matching on the shared envelope shape. Confirmed live: 41 completely unrelated resources (Radar analytics reads -- cloudflare_asn, cloudflare_authority, cloudflare_bot, cloudflare_bot_clas, cloudflare_browser_family, and more -- DNSSEC config, Zero Trust gateway, Magic WAN routes) all bound their own create to the same POST /accounts/{accountId}/v1/images (Images upload), the shortest fewest-param POST anywhere in the spec sharing the generic {result, success} shape. Root cause: findCreate's inline fallback had no path-relationship requirement at all. Fixed by requiring the candidate to sit on a real structural path relationship to the read path (ubx-provider-dynamic PR #61, v1.0.10) -- the same relationship every genuine create/read pair already has. Verified against the real spec: 58 resources affected total (31 correctly lose a false create, 27 rebind to a correct endpoint), zero regressions among 233 unaffected resources.\n\nCross-provider re-check with this second fix (GitHub, Datadog v1/v2, Kubernetes, Azure): all four non-Azure providers stayed clean. Azure was NOT clean -- see the sibling Azure onboarding/investigation notes; this fix caught 5 more affected Azure members beyond the 13 the first fix alone found, bringing Azure's real total to 47 wrong-endpoint resources and 6 wrongly-creatable resources across 16 of 302 members.\n\nSchema regenerated as v1.0.2 (233 resources + 823 data sources, down from 264+820), ubiquex re-pinned, SDK regenerated, categories.json's one newly-orphaned key (cloudflare_location, which turned out to never be a real infra resource at all -- its only prior appearance was this exact bug) removed. All verified independently (checksums, live registry state, specific named cases: the 5 Radar types confirmed data-source-only, cloudflare_image confirmed still a real resource with its own genuine pair). Resuming intro/description authoring against this now-corrected, twice-verified wire-type set."
    }
  ]
}